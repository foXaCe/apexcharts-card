# État de l'overhaul — apexcharts-card

> Fichier d'état durable du skill `ha-lovelace-card-overhaul`. Supprimé au commit final (Phase 9).

## Méta

- **Démarré** : 2026-07-10
- **Branche** : `refactor/card-overhaul-20260710-114007`
- **Tag de backup** : `backup/before-card-overhaul-20260710-114007`
- **Rollback** : `git reset --hard backup/before-card-overhaul-20260710-114007`
- **HA dev** : `/home/stephane/homeassistant` — container docker `homeassistant` (UP)
- **WWW_DIR** : `/home/stephane/homeassistant/config/www` (monté sur `/config/www` — PAS `~/homeassistant/www`, vestige)
- **Ressource Lovelace** : `/local/apexcharts-card.js?v=overhaul-p2` (id `apexcharts_card` dans `.storage/lovelace_resources` — bumper `?v=` à chaque déploiement via docker exec + python, fichier root)
- **MCP chrome-devtools** : indisponible → vérification visuelle en fallback (retour utilisateur avant commit des passes design)

## Phases

- [x] Phase 0 — Pré-flight (git propre, node v22.23.1, container UP, tag + branche créés)
- [x] Phase 0.5 — Recherche web état de l'art ✓ (`/tmp/ha-frontend-state-of-art-2026-07-10.md`, 260 l.)
- [x] Phase 1 — Découverte / inventaire ✓ (baseline build OK 6,5 s, tsc --noEmit 0 erreur)
- [x] Phase 2 — Tooling moderne ✓ (commit `9e9bfe9` — TS 5.9, Rollup 4, ESLint 9 flat, apexcharts 5.16, bundle déployé + ressource enregistrée, HTTP 200)
- [ ] Phase 3 — API carte moderne
- [ ] Phase 4 — Visual editor
- [ ] Phase 5 — Thème Material 3
- [ ] Phase 5.bis — Refonte UI Premium
- [ ] Phase 6 — Accessibilité
- [ ] Phase 7 — Tests Vitest
- [ ] Phase 8 — HACS
- [ ] Phase 9 — Validation finale + rapport

## État de l'art (résumé Phase 0.5)

- **HA local** : core 2026.7.1, frontend `20260624.4` (source vérifiée à ce tag exact)
- **Versions cibles** : Lit 3.3.3 · **TS 5.9.x** (PAS TS 7 — compat toolchain Rollup non garantie) · Rollup 4.62.2 + `@rollup/plugin-typescript` · custom-card-helpers 2.0.0 · home-assistant-js-websocket 9.6.0 · Vitest 4.1.10 + happy-dom 20.10.6 + @open-wc/testing 4.0.0 · apexcharts 5.16.0 · ESLint 9/10 flat config obligatoire · prettier 3.9.x
- **getGridOptions** : graphique → `columns: 12, rows: 'auto', min_columns: 6` (fit-rows seulement si rows numérique ; clamp conditionnel ; DEFAULT_GRID_SIZE={columns:12, rows:'auto'})
- **getEntitySuggestion** (2026.6) : champ de `window.customCards`, pas méthode statique — déjà implémenté dans le fork
- **Breaking 2026.5→2026.7** : migration Material → Web Awesome (`--mdc-*` → `--ha-*`, ha-radio/ha-drawer réécrits, ha-top-app-bar supprimé, tailles ha-button xs/s/m/l/xl) — ne pas dépendre des internals
- **Theming** : uniquement variables sémantiques HA (--primary-color etc.) ; tokens sections `--ha-section-grid-row-height` 56px / gaps 8px ; `--ha-card-border-radius` (fallback `--ha-border-radius-lg`)
- **HACS** : dist/<repo-name>.js, hacs.json (name, render_readme, filename, homeassistant), releases recommandées
- **Anti-patterns** : rows numérique sur contenu variable ; getLayoutOptions déprécié ; rollup-plugin-terser/typescript2 dépréciés ; double bundle Lit ; validation config non factorisée setConfig/assertConfig

## Inventaire (résumé Phase 1)

- **Carte** : ApexCharts Card (`custom:apexcharts-card`) v2.3.0 — fork foXaCe de RomRider
- **Stack** : Lit 3.3.1 (déjà moderne) / TS 4.9.5 (à migrer 5.x) / Rollup 2.79.2 via rollup-plugin-typescript2 + babel preset-env (à migrer Rollup 4 ES2022) / ESLint 8 legacy
- **Niveau estimé** : Mid (hybride) — cœur RomRider vieillissant + couche fork moderne (éditeur visuel Lit+ha-form, getGridOptions, getEntitySuggestion, getStubConfig, hass setter filtré, CI durcie)
- **Déjà en place** : getGridOptions (section_mode), getEntitySuggestion (par state_class), getStubConfig élaboré, éditeur visuel par onglets ha-form/ha-selector (~4800 l., src/editor/), hass setter filtré par référence d'entité, HACS ok, CI complète (release-please, CodeQL, osv-scanner, gitleaks)
- **Dette clé** : TS 4.9 / Rollup 2 / ESLint 8 ; moment.js×4 packages ; ~20 hex hard-codés dans styles.ts (953 l., risque dark mode) ; 0 test ; @media sans @container ; ARIA quasi absent carte principale ; bundle 1,7 Mo (~300 Ko gz) — 150 locales ApexCharts importées statiquement ; `tinycolor@0.0.1` vestige + @ctrl/tinycolor ; apexcharts ^5.3.3 vs 5.16.0 ; main: pointe src/.ts ; validation config via ts-interface-checker (types-config-ti.ts généré)
- **Compat à préserver** : clé legacy `entities`→`series` dans setConfig ; card_mod/style/uix/ll_template ; corpus de non-régression = test/ui-lovelace.yaml (51 blocs de config)
- **Configs utilisateur HA dev** : 0 trouvée (aucun dashboard n'utilise la carte) → non-régression basée sur test/ui-lovelace.yaml
- **Éditeur** : déjà moderne (variables HA, zéro hex) — la modernisation aligne le CŒUR sur le niveau de l'éditeur

## Décisions design

_(à remplir)_

## Verdicts visuels

_(à remplir)_

## TODO restants

_(à remplir)_
